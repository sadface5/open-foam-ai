# Python packages this project needs.
# Install them all with:  pip install -r requirements.txt
#
# (Using ">=" means pip installs the latest compatible version.)

anthropic>=0.40.0        # Official Claude API client
PySide6>=6.6.0           # The desktop GUI toolkit (Qt for Python)
scikit-learn>=1.3.0      # Used for the local knowledge search (RAG)
python-dotenv>=1.0.0     # Loads your API key from the .env file
PyYAML>=6.0              # Loads the structured 10-step skill definitions

# Optional: only needed to build the Windows .exe (see build_windows_exe.bat)
pyinstaller>=6.0.0

# Optional: only needed for the older browser-based interface (legacy app.py)
streamlit>=1.30.0
